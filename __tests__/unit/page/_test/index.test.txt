import { render, screen, fireEvent } from '@testing-library/react'
import Index from '../../../../pages/index'
import '@testing-library/jest-dom'

describe('A2_Page > Index', () => {
  it('[A2_3_1]レンダリングされているか', () => {
    const { container, debug } = render(<Index />)
    debug()
  })
})
